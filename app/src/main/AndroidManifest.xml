<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.spacemonster.book.mentors">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mentors"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_mentors_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" />
        <activity android:name=".NewsAllActivity" />
        <activity android:name=".NewsViewActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".UserInfoActivity" />
        <activity android:name=".SnsViewActivity"/>
        <activity android:name=".ChangeSeat.Change_Busan_Geoje_Activity"/>
    </application>

</manifest>